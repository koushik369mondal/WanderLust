# Cloudinary Configuration (for image uploads)
# Sign up at https://cloudinary.com/ and get your credentials from the dashboard
CLOUD_NAME=your_cloudinary_name
CLOUD_API_KEY=your_cloudinary_api_key
CLOUD_API_SECRET=your_cloudinary_api_secret

# Mapbox Configuration (for interactive maps)
# Sign up at https://account.mapbox.com/ and get your access token
MAP_TOKEN=your_mapbox_public_token

# MongoDB Configuration
# For MongoDB Atlas: Get connection string from your cluster
# For local MongoDB: use mongodb://localhost:27017/wanderlust
ATLAS_DB_URL=your_mongodb_connection_string

# Session Secret (use a strong random string for security)
# Generate a secure random string for session management
SECRET=your_super_secret_session_key