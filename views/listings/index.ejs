<% layout('/layouts/boilerplate') %>

    <style>
        /* New gradient filter bar */
        #filters {
            display: flex;
            align-items: center;
            overflow-x: auto;
            padding: 1rem;
            gap: 1rem;
            margin-top: 10px;
            border-radius: 20px;
            background:var(--bg-primary);
            color: white;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        /* Navbar */
        /* .navbar {
            backdrop-filter: blur(12px);
            background: var(--bg-primary);
        } */

        .filter {
            text-align: center;
            opacity: 0.9;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-width: 70px;
            flex-shrink: 0;
            transition: background 0.3s ease, transform 0.2s ease;
            border-radius: 12px;
            padding: 6px 10px;
        }

        .filter:hover {
            opacity: 1;
            background: rgba(255, 255, 255, 0.15);
            cursor: pointer;
            transform: translateY(-2px);
        }

        /* Add this new style for active filter */
        .active-filter {
            background: rgba(255, 255, 255, 0.3);
            opacity: 1;
            transform: translateY(-2px);
            font-weight: bold;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }
        
        .filter i {
            font-size: 1.3rem;
            color: white;
            margin-bottom: 0.3rem;
        }

        .filter p {
            font-size: 0.8rem;
            margin: 0;
            color: white;
            white-space: nowrap;
        }

        /* tax toggle restyled */
        .tax-toggle {
            border: 1px solid rgba(255, 255, 255, 0.7);
            border-radius: 1rem;
            padding: 0.5rem 1rem;
            background: rgba(255, 255, 255, 0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            margin-left: auto;
            color: white;
        }

        /* Hide scrollbar */
        #filters::-webkit-scrollbar {
            display: none;
        }

        /* Card refinements */
        .listing-card {
            border: none !important;
            border-radius: 1.2rem !important;
            overflow: hidden;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .listing-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 22px rgba(0, 0, 0, 0.15);
        }

        .listing-image-container {
            height: 18rem;
        }

        .listing-card .card-body {
            padding: 1rem;
        }

        /* .listing-card .card-body b {
            color: #777;
        } */
    </style>


    <!-- Replace the filter div section with this updated code -->
    <div id="filters">
        <div class="tax-toggle">
            <div class="form-check-reverse form-switch">
                <input class="form-check-input tax-input" type="checkbox" role="switch" id="switchCheckDefault" />
                <label class="form-check-label" for="switchCheckDefault"><b>Display total after taxes</b></label>
            </div>
        </div>
    </div>
    <a href="/listings" class="filter <%= !activeCategory ? 'active-filter' : '' %>">
        <i class="fa-solid fa-border-all"></i>
        <p>All</p>
    </a>
    <a href="/listings?category=trending" class="filter <%= activeCategory === 'trending' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-fire"></i>
        <p>Trending</p>
    </a>
    <a href="/listings?category=rooms" class="filter <%= activeCategory === 'rooms' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-bed"></i>
        <p>Rooms</p>
    </a>
    <a href="/listings?category=iconic_cities" class="filter <%= activeCategory === 'iconic_cities' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-mountain-city"></i>
        <p>Iconic Cities</p>
    </a>
    <a href="/listings?category=mountains" class="filter <%= activeCategory === 'mountains' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-mountain"></i>
        <p>Mountains</p>
    </a>
    <a href="/listings?category=castles" class="filter <%= activeCategory === 'castles' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-chess-rook"></i>
        <p>Castles</p>
    </a>
    <a href="/listings?category=amazing_pools" class="filter <%= activeCategory === 'amazing_pools' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-person-swimming"></i>
        <p>Amazing pool</p>
    </a>
    <a href="/listings?category=camping" class="filter <%= activeCategory === 'camping' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-tents"></i>
        <p>Camping</p>
    </a>
    <a href="/listings?category=farms" class="filter <%= activeCategory === 'farms' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-tractor"></i>
        <p>Farms</p>
    </a>
    <a href="/listings?category=arctic" class="filter <%= activeCategory === 'arctic' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-snowflake"></i>
        <p>Arctic</p>
    </a>
    <a href="/listings?category=domes" class="filter <%= activeCategory === 'domes' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-igloo"></i>
        <p>Domes</p>
    </a>
    <a href="/listings?category=boats" class="filter <%= activeCategory === 'boats' ? 'active-filter' : '' %>">
        <i class="fa-solid fa-ship"></i>
        <p>Boats</p>
    </a>
</div>


    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 mt-3">
        <% for(let listing of allListings) { %>
            <div class="col mb-4">
                <a href="/listings/<%= listing._id %>" class="listing-link">
                    <div class="card listing-card h-100">
                        <div class="listing-image-container">
                            <img src="<%= listing.image && listing.image.url ? listing.image.url : 'https://via.placeholder.com/300' %>" class="card-img-top listing-image"
                                alt="listing_image" />
                        </div>
                        <div class="card-body">
                            <p class="card-text">
                                <b>
                                    <%= listing.title %>
                                </b><br />
                                <%= typeof listing.price !== 'undefined' ? listing.price.toLocaleString("en-IN") : 'N/A' %> / night
                                    <i class="tax-info">&nbsp; &nbsp; +18% GST</i>
                            </p>
                        </div>
                    </div>
                </a>
            </div>
            <% } %>
    </div>

    <script>
        let taxSwitch = document.getElementById("switchCheckDefault");
        taxSwitch.addEventListener("click", () => {
            let taxInfo = document.getElementsByClassName("tax-info");
            for (info of taxInfo) {
                info.style.display = (info.style.display !== "inline") ? "inline" : "none";
            }
        });
    </script>