# Test Environment Variables
NODE_ENV=test
PORT=3001

# MongoDB Test Database
ATLASDB_URL=mongodb://localhost:27017/wanderlust_test

# Session Secret (for testing)
SECRET=test_secret_key_for_testing_only

# Cloudinary (Mock/Test credentials)
CLOUD_NAME=test_cloud
CLOUD_API_KEY=test_api_key
CLOUD_API_SECRET=test_api_secret

# Map Token (can be empty for tests)
MAP_TOKEN=

# Disable external services in tests
DISABLE_SEEDING=true
